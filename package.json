{
  "name": "pda-boracay-shared",
  "version": "5.7.0",
  "description": "Shared API constants, types, and hooks for PDA Boracay apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run src/api.contract.test.ts",
    "test:smoke": "vitest run src/api.smoke.test.ts",
    "test:all": "vitest run"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "@mui/material": ">=6.0.0"
  },
  "devDependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/material": "^6.5.0",
    "@types/react": "^18.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.1.0"
  }
}
